{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd salemanager-backend && npm install && npm run build && npx prisma generate",
    "watchPaths": ["salemanager-backend/**"]
  },
  "deploy": {
    "startCommand": "cd salemanager-backend && npm run prisma:generate && npm run start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}
